---
  title: Dummy Post
  summary: This is a very dummy post. It's only about testing things. Don't mind this!
  publishedAt: '2024-08-09'
  categories: ["Testing", "Markdown", "Highlight.js"]
---

This post is only about testing things. Let's see how it goes.

# Testing Highlight.js

First of all, let's test the syntax highlighting.

```python
# Import required libraries
import sys
import os
import json
import time

# Define some constants
MAX_RETRIES = 3
DATA_PATH = "data/elements.json"

# Main function
def main():
    print("Hello, World!")
    
    # Load configuration
    config = load_config()
    
    # Initialize system
    init_system()
    
    # Process data
    process_elements()

# Run program
if __name__ == "__main__":
    main()
```

```java
public class HelloWorld {
    public static void main(String[] args) {
        System.out.println("Hello, World!");
    }
}
```

As we can see,

```rust
fn main() {
    println!("Hello, World!");
}
```

This is good!

```js
class HelloWorld {
    constructor() {
        console.log("Hello, World!");
    }
}
```

# Testing GitHub Flavored Markdown

## Autolink literals

www.example.com, https://example.com, and contact@example.com.

## Footnote

A note[^1]

[^1]: Big note.

## Strikethrough

~one~ or ~~two~~ tildes.

## Table

| a | b  |  c |  d  |
| - | :- | -: | :-: |

## Tasklist

* [ ] to do
* [x] done